<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 变量指向根路径下的logs文件夹 -->
    <property name="LOG_PATH" value="logs" />
    
    <!-- 路径1：控制台打印 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- 路径2：INFO级别文件写入 -->
    <appender name="INFO_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/app_info.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/app_info-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- 路径3：ERROR级别文件写入 -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/app_error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/app_error-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>60</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- ================================ -->
    <!-- 开发环境配置 (dev) -->
    <!-- ================================ -->
    <springProfile name="dev">
        <root level="DEBUG">
            <!-- 开发环境只输出到控制台 -->
            <appender-ref ref="CONSOLE" />
        </root>
        
        <!-- 可以为特定包设置更详细的日志 -->
        <logger name="com.example" level="TRACE" additivity="false">
            <appender-ref ref="CONSOLE" />
        </logger>
    </springProfile>
    
    <!-- ================================ -->
    <!-- 生产环境配置 (prod) -->
    <!-- ================================ -->
    <springProfile name="prod">
        <root level="INFO">
            <!-- 生产环境输出到控制台+文件 -->
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="INFO_FILE" />
            <appender-ref ref="ERROR_FILE" />
        </root>
        
        <!-- 生产环境关闭某些框架的详细日志 -->
        <logger name="org.springframework" level="WARN" />
        <logger name="org.hibernate" level="WARN" />
    </springProfile>
    
    <!-- ================================ -->
    <!-- 测试环境配置 (test) - 可选 -->
    <!-- ================================ -->
    <springProfile name="test">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="INFO_FILE" />
        </root>
    </springProfile>
    
    <!-- ================================ -->
    <!-- 默认配置（没有指定profile时使用） -->
    <!-- ================================ -->
    <springProfile name="default">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>
</configuration>